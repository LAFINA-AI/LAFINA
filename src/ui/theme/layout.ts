/**
 * Layout constants — replaces raw Layout object in old theme.ts.
 */
export const Layout = {
  borderRadiusCard: 16,
  borderRadiusPill: 999,
  borderRadiusButton: 12,
  navbarHeight: 72,
  micButtonSize: 60,
} as const;
