/**
 * Shadows — Android-only elevation values.
 * LAFINA is Android-only; iOS shadow properties have been removed.
 */
export const Shadows = {
  card: {
    elevation: 4,
  },
  navbar: {
    elevation: 8,
  },
  micButton: {
    elevation: 12,
  },
} as const;
